en = encoded

If you the name of the flag file it's => flag.txt.en

The file is encoded, simply run the given python program and pass the flag file and -d as n argument to decode the encrypted flag file.

-d argument is for decoding.(check the python code, you'll understand it)

: )
